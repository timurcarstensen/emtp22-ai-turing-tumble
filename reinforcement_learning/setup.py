from setuptools import setup

setup(
    name="bugbit_v0",
    version="1.0.0",
    install_requires=["gym"]
)

setup(
    name="connectfourmvc_v0",
    version="1.0.0",
    install_requires=["gym"]
)
